spring:
  application:
    name: event-pipeline

  datasource:
    url: jdbc:postgresql://192.168.50.1:5432/event_pipeline
    username: event_user
    password: event_password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true